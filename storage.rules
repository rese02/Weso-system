rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow write access only for authenticated users
    // This covers guest document and payment proof uploads.
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
